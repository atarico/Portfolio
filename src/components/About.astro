---
const stack = ['TypeScript', 'React', 'Astro', 'Node.js', 'Supabase'];

interface Stat {
  key: string;
  value: string | string[] | number;
}

const stats: Stat[] = [
  { key: 'ubicación',   value: 'Buenos Aires, Argentina'  },
  { key: 'rol_actual',  value: 'Frontend Dev'             },
  { key: 'stack',       value: stack                      },
  { key: 'educación',   value: 'Ing. Sistemas (en curso)' },
  { key: 'café_por_día', value: 3                         },
];
---

<section id="about" class="about-section" aria-labelledby="about-title">
  <div class="about-inner">

    <h2 id="about-title" class="sr-only">Sobre mí</h2>

    <!-- ── Título terminal ── -->
    <header class="about-header reveal">
      <p class="section-label">
        <span class="label-prefix">$</span> cat about.md
      </p>
    </header>

    <!-- ── Grid dos columnas ── -->
    <div class="about-grid">

      <!-- Columna izquierda: texto -->
      <div class="about-text reveal" style="transition-delay: 120ms">

        <p class="about-paragraph">
          Soy desarrollador web enfocado en el ecosistema
          JavaScript/TypeScript. Actualmente curso Ingeniería en Sistemas en
          la UTN y fui profesor de Programación III en la Tecnicatura Superior
          en Programación de la misma universidad.
        </p>

        <p class="about-paragraph">
          Dar clases me entrenó para explicar cosas complejas de manera
          simple. Eso mismo aplico en los equipos donde trabajo: comunicar
          decisiones técnicas sin vueltas.
        </p>

        <p class="about-paragraph">
          Trabajo con React, Astro, Next.js, Node.js y Supabase. Lo que más
          disfruto es llevar ideas a producción: una app de geolocalización
          de farmacias, un sistema de entradas para un centro cultural, un
          gestor de llaves para la UTN, una web para un broker de seguros.
        </p>
      </div>

      <!-- Columna derecha: ventana de terminal -->
      <div class="reveal" style="transition-delay: 260ms">
        <div class="terminal-window">

          <!-- Barra de título -->
          <div class="terminal-titlebar">
            <span class="dot dot-red"   aria-hidden="true"></span>
            <span class="dot dot-yellow" aria-hidden="true"></span>
            <span class="dot dot-green"  aria-hidden="true"></span>
            <span class="terminal-title">atarico@portfolio: ~</span>
          </div>

          <!-- Cuerpo de la terminal -->
          <div class="terminal-body">

            <!-- Prompt del comando -->
            <p class="terminal-line">
              <span class="t-prompt">&gt;&nbsp;</span>
              <span class="t-cmd">system.fetch</span>
              <span class="t-paren">(</span>
              <span class="t-string">"atarico/stats"</span>
              <span class="t-paren">)</span>
            </p>

            <!-- Output JSON -->
            <div class="t-output">
              <p class="t-brace">&#123;</p>

              {stats.map((stat, i) => (
                <p class="t-prop" style={`animation-delay: ${200 + i * 120}ms`}>
                  <span class="t-key">"{stat.key}"</span>
                  <span class="t-colon">:&nbsp;</span>

                  {Array.isArray(stat.value) ? (
                    <span class="t-array">
                      [&thinsp;
                      {stat.value.map((v, j, arr) => (
                        <span>
                          <span class="t-string">"{v}"</span>
                          {j < arr.length - 1 && <span class="t-colon">,&nbsp;</span>}
                        </span>
                      ))}
                      &thinsp;]
                    </span>
                  ) : typeof stat.value === 'number' ? (
                    <span class="t-number">{stat.value}</span>
                  ) : (
                    <span class="t-string">"{stat.value}"</span>
                  )}

                  {i < stats.length - 1 && <span class="t-colon">,</span>}
                </p>
              ))}

              <p class="t-brace">&#125;</p>
            </div>

            <!-- Prompt idle -->
            <p class="terminal-line t-idle">
              <span class="t-prompt">&gt;&nbsp;</span>
              <span class="cursor-blink t-cursor-idle" aria-hidden="true"></span>
            </p>
          </div>

        </div>
      </div>

    </div>
  </div>
</section>

<script>
  const targets = document.querySelectorAll<HTMLElement>('#about .reveal');

  const observer = new IntersectionObserver(
    (entries) => {
      entries.forEach((entry) => {
        if (entry.isIntersecting) {
          (entry.target as HTMLElement).classList.add('is-visible');
          observer.unobserve(entry.target);
        }
      });
    },
    { threshold: 0.15 }
  );

  targets.forEach((el) => observer.observe(el));
</script>

<style>
  /* ── Sección ── */
  .about-section {
    padding: 2rem 1.5rem;
  }

  .about-inner {
    max-width: 64rem;
    margin: 0 auto;
    display: flex;
    flex-direction: column;
    gap: 3rem;
  }

  /* ── Título terminal ── */
  .about-header {
    display: flex;
    flex-direction: column;
    gap: 0.25rem;
  }

  /* .section-label y .label-prefix viven en global.css.
     About es la única sección con el prefijo sin bold. */
  .label-prefix {
    font-weight: 400;
  }

  /* ── Grid ── */
  .about-grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: 2.5rem;
  }

  @media (min-width: 768px) {
    .about-grid {
      grid-template-columns: 1fr 1fr;
      align-items: start;
      gap: 3.5rem;
    }
  }

  /* ── Texto del perfil ── */
  .about-paragraph {
    font-size: clamp(1rem, 2vw, 1.1rem);
    color: var(--text-muted);
    line-height: 1.85;
    margin-bottom: 1.1rem;
  }

  .about-paragraph:last-child {
    margin-bottom: 0;
  }

  /* ── Ventana de terminal ── */
  .terminal-window {
    border: 1px solid var(--border-accent);
    border-radius: 8px;
    overflow: hidden;
    box-shadow: var(--glow-sm), 0 16px 40px rgba(0, 0, 0, 0.5);
    font-size: 0.82rem;
  }

  /* .terminal-titlebar y .dot* viven en global.css */

  .terminal-title {
    margin-left: 0.5rem;
    font-size: 0.72rem;
    color: var(--text-muted);
    letter-spacing: 0.04em;
  }

  /* Cuerpo */
  .terminal-body {
    padding: 1.1rem 1.25rem 1rem;
    background: rgba(10, 10, 10, 0.97);
    display: flex;
    flex-direction: column;
    gap: 0.2rem;
  }

  .terminal-line {
    display: flex;
    align-items: baseline;
    flex-wrap: wrap;
  }

  /* Output JSON */
  .t-output {
    margin: 0.5rem 0 0.3rem 0.4rem;
    display: flex;
    flex-direction: column;
    gap: 0.15rem;
  }

  .t-brace {
    color: var(--text-primary);
  }

  .t-prop {
    display: flex;
    flex-wrap: wrap;
    align-items: baseline;
    padding-left: 1.2rem;
  }

  /* La animación de props solo se activa cuando hay JS disponible */
  :global(html.js) .t-prop {
    opacity: 0;
    animation: propReveal 0.4s ease forwards;
  }

  /* Colores sintaxis */
  .t-prompt  { color: var(--text-accent); text-shadow: var(--glow-sm); font-weight: 700; }
  .t-cmd     { color: #61afef; }
  .t-paren   { color: #abb2bf; }
  .t-string  { color: #98c379; }
  .t-key     { color: #e06c75; }
  .t-number  { color: #d19a66; }
  .t-colon   { color: #abb2bf; }
  .t-array   { color: #abb2bf; }

  /* Cursor idle */
  .t-idle {
    margin-top: 0.35rem;
  }

  .t-cursor-idle {
    display: inline-block;
    width: 0.5em;
    height: 0.85em;
    background-color: var(--text-accent);
    vertical-align: text-bottom;
    box-shadow: var(--glow-sm);
  }

  /* ── Reveal: base compartida en global.css ──
     Variación local: transición más lenta y desplazamiento más amplio.
     Los selectores con #about ganan en especificidad a la base global. */
  #about .reveal {
    transition:
      opacity  0.6s cubic-bezier(0.22, 1, 0.36, 1),
      transform 0.6s cubic-bezier(0.22, 1, 0.36, 1);
  }

  :global(html.js) #about .reveal {
    transform: translateY(22px);
  }

  :global(html.js) #about .reveal.is-visible {
    transform: translateY(0);
  }

  /* ── Animación de props del JSON ── */
  @keyframes propReveal {
    from { opacity: 0; transform: translateX(-6px); }
    to   { opacity: 1; transform: translateX(0);    }
  }

  /* El trigger de propReveal arranca solo cuando la ventana es visible */
  :global(html.js) .reveal:not(.is-visible) .t-prop {
    animation: none;
  }
</style>
